<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nexon.maple.comment.repository.CommentDao">

    <!-- 캐릭터 정보 조회 -->
    <select id="findByCharacterId" parameterType="Long" resultType="com.nexon.maple.comment.entity.CommentInfo">
        SELECT
            ID, CHARACTER_ID characterId, USER_ID userId, COMMENT, CREATED_AT createAt
        FROM COMMENT_INFO
        WHERE CHARACTER_ID = #{value}
        ORDER BY CREATED_AT DESC
    </select>

    <insert id="save" parameterType="com.nexon.maple.comment.entity.CommentInfo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO COMMENT_INFO(CHARACTER_ID, USER_ID, COMMENT, CREATED_AT)
        VALUES(#{characterId}, #{userId}, #{comment}, #{createdAt})
    </insert>

</mapper>